<UserControl x:Class="GarageOrganizer.WPF.Controls.ControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GarageOrganizer.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Column="0" Text="Search" Padding="10" Margin="0 0 10 0"/>
        <Button Grid.Column="1" Padding="10">
            <Image Source="/Images/search.png" Width="20" Margin="10 0"/>
        </Button>
        <Button Grid.Column="2" Padding="10" Margin="10 0 10 0">
            <Image Source="/Images/plus.png" Width="20" Margin="10 0"/>
        </Button>
    </Grid>
</UserControl>
